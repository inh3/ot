{
    "name": "ot",
    "version": "0.0.1",

    "description": "ot",

    "scripts": {
        "preinstall": "npm install -g grunt-cli"
    },

    "dependencies": {
        "mariasql": ">= 0.1.19",
        "underscore": ">= 1.5.1",
        "express": ">= 3.4.4"
    },

    "devDependencies": {
        "grunt": ">=0.4.0",
        "grunt-contrib-handlebars": ">=0.5.8"
    },

    "engines": {
        "node": ">= 0.10.0"
    }
}